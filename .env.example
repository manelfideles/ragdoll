# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# Needed for exact token counts, which the routing decision depends on.
# Without it, ragdoll falls back to an approximate count and says so.
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
